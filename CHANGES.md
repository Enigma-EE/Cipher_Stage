# 变更记录（整合上游）

本文件记录将 `swordswind/ai_virtual_mate_web`（GPL-3.0）相关代码/思路整合进本项目（MIT）的变更事项与影响范围。

## 2025-11-15 初始整合准备
- 在 `NOTICE.md` 添加上游 `ai_virtual_mate_web` 的致谢与许可信息（GPL-3.0）。
- 规划整合路径：尽量以“独立服务/模块”方式引入，避免直接混合导致全仓库 GPL 传染；如确需合并代码，则接受 GPL-3.0 要求并在分发时提供完整源代码。
- 建立 `docs/INTEGRATION.md`，描述整合范围、构建与合规事项。

## 拟整合模块范围（计划）
- ASR：评估引入上游 FunASR ONNX 的离线识别作为补充；以独立服务或插件方式接入。
- MMD 展示：作为独立前端页面与后端路由接入（资产遵循其各自许可，不随仓库分发）。
- 语音表情映射：参考上游的情绪映射策略，融入现有 TTS/表情管线。
- 长期记忆：对比上游 `data/history` 与本仓库 `memory/` 体系，择优融合或互通。

备注：如实际合并上游源代码文件，必须在每个被修改的文件头或本文件中明确记录改动范围与来源，并保留原始版权与 LICENSE。